syntax = "proto3";
package block;

message Metadata {
  // number of records per segment
  int64 segmentSize = 1;

  // number of segment files in use
  int64 segmentCount = 2;

  // total number of records in use
  // calculated across all segments
  int64 recordCount = 3;
}
