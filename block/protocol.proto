syntax = "proto3";
package block;

message Metadata {
  // records per segment
  uint32 capacity = 1;

  // total segment files
  uint32 segments = 2;

  // total records in use
  uint32 records = 3;
}
